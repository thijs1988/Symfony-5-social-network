{% extends 'base.html.twig' %}

{% block body %}
<div class="col-6 offset-3">
    <div class="my-3 p-3 bg-white rounded box-shadow">
    {{ form_start(form) }}

    {{ form_widget(form) }}

    {{ form_end(form) }}
    </div>
</div>
{% endblock %}

